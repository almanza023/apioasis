name: Deploy Laravel to FTP

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp.comercializadoraoasis.com
          username: comercializadoraoasis.com
          password: Av(@}uY#5kv2
          server-dir: /api/  # Carpeta de destino en el servidor FTP
          local-dir: ./      # Carpeta local a subir (todo el repo)

